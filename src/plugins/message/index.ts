export { listenMessage, MessageData, MessageType } from "@base/message";
// import Vue from "vue";

// const Plugin = {
//     install() {
//         listenMessage((data, e) => {});
//     }
// };

// Vue.use(Plugin);

// export { listenMessage, MessageData, MessageType };
